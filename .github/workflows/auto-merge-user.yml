name: Auto Merge Syncer PRs

on:
  pull_request:
    types: [opened, reopened, synchronize]

permissions:
  contents: write
  pull-requests: write

jobs:
  auto-merge:
    runs-on: ubuntu-latest

    steps:
      - name: Debug PR Author
        run: echo "${{ github.event.pull_request.user.login }}"
